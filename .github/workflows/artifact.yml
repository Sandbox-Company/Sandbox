name: Upload Artifact

on:
  push:

jobs:
  artifact-job:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello Artifact" > test.txt

      - uses: actions/upload-artifact@v4
        with:
          name: sample-artifact
          path: test.txt
